name: List Repository Files

on:
  push:
    branches: [main, master]

jobs:
  list-files:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2 # or v3 or v4

    - name: List files
      run: |
        Get-ChildItem -Path . -Recurse
      shell: pwsh
